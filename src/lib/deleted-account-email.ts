/**
 * Reserved domain for the anonymised address written by an approved member
 * deletion. This leaf stays client-safe because both deletion predicates and
 * client-rendered eligibility controls need the same marker.
 */
export const DELETED_CONTACT_EMAIL_DOMAIN = "deleted.invalid";

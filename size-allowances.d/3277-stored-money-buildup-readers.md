# File-size allowances for #3277

file: src/lib/booking-edit-guest-ranges.ts
lines: 1947
reason: the operation grain is decided beside the existing surrendered and
  added-night sets; extracting seven lines would separate the evidence rule
  from the only calculation that proves whether the whole strand moved.

file: src/lib/booking-guest-removal-service.ts
lines: 1401
reason: the canonical input must be loaded inside this writer's existing locks
  before its destructive guest delete, while selection must remain beside the
  established derived amount; extracting either half would obscure that
  ordering contract without removing any policy.

file: src/lib/xero-booking-invoices.ts
lines: 1435
reason: the aggregate selection must remain between the existing manual-paid
  fence and the first provider call, and its history must remain on the unique
  Xero operation assembled in this function; a split would hide both ordering
  guarantees behind another call.

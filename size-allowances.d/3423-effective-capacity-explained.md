file: src/app/(admin)/admin/lodges/[id]/page.tsx
lines: 605
reason: Three lines beyond the base, and all of them are the capacity field's own wiring. The #2724 change itself left this file SHORTER than it found it — the guidance moved out to `src/components/admin/lodge-capacity-guidance.tsx` — and no allowance was owed. The fix round put back what the fix needs and nothing else: the lodge's active DOUBLE bed count in state, so the guidance can preview the partner-shared headroom for an unsaved capacity instead of telling an officer the surplus above the beds is inert; the `aria-describedby` that attaches the guidance to the field, which is what a view-only officer and an officer tabbing back have instead of typing; the `id` on the fallback hint it names; and the import of the shared save bounds the field's `min`/`max` and its save now read. Every one of those is a prop or an attribute on the control it belongs to, so there is nowhere else for it to live: the component already holds all the arithmetic and all the prose, and the two figures it needs arrive here because this shell is what fetches them. Splitting a 600-line route shell is real work and the right work eventually — the seam is between its five cards, not around four lines of one field.

Renumbered the school-organisation migration so it applies after the audit-category
backfill that reached `main` while this epic was building, rather than before it.
Nothing about the migration itself changes — only its name, and the ledger sentence
explaining why the name moved.
